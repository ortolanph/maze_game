 self.images["obstacles"]["rock-large"] = pygame.image.load(
            os.path.join(img_skin_folder, obstacles["rock-large"]))
        self.images["obstacles"]["cross-arm"] = pygame.image.load(os.path.join(img_skin_folder, obstacles["cross-arm"]))
